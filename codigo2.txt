program teste; {programa 1 _)T ; @#  exemplo}
var
    y:real;

procedure adicao(var x:real);
		var
            a, b: real;
		Begin
            a:=1;
            b:=2;
			x:=a+b;
			writeln(x);
		end;

procedure adicao;
        var
            x, a, b: real;
		Begin
            a:=1;
            b:=2;
			x:=a+b;
			writeln(x);
		end;

begin
    y:=4;
    writeln(y);
    adicao(y);
    adicao;
    writeln(y);
end.
