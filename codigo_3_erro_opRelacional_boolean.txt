program ErroRelacionalBoolean;
var
    y:real;
procedure adicao(var i:real);
		begin
            //ERRO
            while true < false do
                    begin
                        i := i + 1;
                    end;
		end;
begin
    adicao(y);
end.